<h1> Hellooooo <%= @user.email %> </h1>

<% if @user.atts_table != nil %>
    <% @atts_table = AttsTable.find(@user.atts_table) %>
    <%= link_to "Your Preferences", atts_table_path(@atts_table)%>
<% else %>
    <%= link_to "Enter Some Information about yourself to start getting matches", edit_user_path(@user) %>
<% end %>

<br>

<h2> Your Matches </h2>
<%= link_to "Your Matches", user_matches_path(@user.id) %>
